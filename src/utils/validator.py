def estDeplacementValide(plateau, joueur, position_depart, position_arrivee):
    """
    Vérifie si un déplacement de pion est valide.
    
    Args:
        plateau (Plateau): Représentation actuelle du plateau de jeu.
        joueur (Joueur): Joueur effectuant le déplacement.
        position_depart (tuple): Position de départ du pion (x, y).
        position_arrivee (tuple): Position d'arrivée du pion (x, y).
    
    Returns:
        bool: True si le déplacement est valide, False sinon.
    """
    pass

def estCaptureValide(plateau, joueur, position_depart, position_arrivee):
    """
    Vérifie si une capture de pion est valide.
    
    Args:
        plateau (Plateau): Représentation actuelle du plateau de jeu.
        joueur (Joueur): Joueur effectuant la capture.
        position_depart (tuple): Position de départ du pion (x, y).
        position_arrivee (tuple): Position d'arrivée du pion (x, y).
    
    Returns:
        bool: True si la capture est valide, False sinon.
    """
    pass